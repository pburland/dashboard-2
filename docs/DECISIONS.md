# Decisions

The rebuild inherits Felipe's architecture and swaps in Patrick's data and
goals. Where Felipe's build and Patrick's ARCHITECTURE.md disagreed,
Felipe's wins unless it couldn't meet a stated requirement; each override
is listed with the reason.

## Stack
- **Python + Railway** (Felipe). Web service now; scheduled jobs (nightly
  ingest, 05:30 morning check, Sunday summary) arrive with ingest.
- **Supabase Postgres** (both). Plain SQL migrations, applied on every
  deploy by `scripts/migrate.py`.
- **FastAPI instead of `http.server`.** Felipe's server served its whole
  directory, `config/credentials.json` included, over a public tunnel with
  no auth. This one serves explicit routes only.

## Sources (Patrick's decision, 2026-09-23)
- **Garmin** for all endurance activities, via `garminconnect` >= 0.3.16.
  `garth` (Felipe's library) was deprecated in March 2026. The server never
  logs in; it renews a token created on Patrick's Mac and stores each
  renewed token in `integrations`.
- **Hevy API** for lifting (per-set, for the progression engine).
- **Oura** for sleep, readiness, HRV, resting HR, temperature. OAuth2
  (personal tokens were retired in Dec 2025). Garmin's own sleep/HRV is
  ignored so two devices never disagree.
- **Open-Meteo** for weather. No Strava.

## Kept from Felipe
Schema shape (activities + laps, date-keyed recovery, raw payloads, typed
weekly notes, RLS on with no policies); pipeline stages; server-side chat
proxy; Friel period structure and concurrent-strength rules
(`app/periodization/friel.py`); standing rules learned from compliance;
the UI and PWA approach (arrives in the phone step).

## Overrides of Felipe's build
- **Code decides the plan; the model explains it.** Felipe's prescriptions
  were generated by the model from prompt text, so nothing enforced a
  health hold except the prompt. Here the health gate and the validator
  (`app/analysis/validator.py`) run before anything is shown.
- **Phase table in the database with contiguity enforced** (exclusion
  constraint + deferred trigger). Felipe's lookup silently fell back to
  "Build" for unknown phase names, the same bug class as the prototype's gap.
- **One clock** (`app/clock.py`). Felipe called `datetime.now()` in 82 places.
- **Load from heart rate (TRIMP)**, the same for every source, since the
  Garmin CSV has no training-load column.

## Kept from Patrick's ARCHITECTURE.md over Felipe's schema
Per-set `strength_sets`; `phases` / `phase_days` / `planned_workouts`
instead of one JSON blob; `integrations` for rotating tokens;
`conversations` / `messages` with context snapshots; `weekly_summaries`.

## Added
`health_episodes` (at most one open), `flags`, `goals`, `push_subscriptions`.

## Numbers worth knowing
- Decoupling uses Friel's first-half vs second-half efficiency factor. The
  Sep 13 run (HR 133 → 153 at equal pace) is ~7% by this method, not the
  15-16% first-mile-to-last-mile rise; it is still over the 5% limit.
- Heat thresholds in `app/analysis/heat.py` are this system's conservative
  defaults, not a published standard, and drop 5°F during a return.
- Max HR 190 is observed (Sep 1), not tested. No zones exist until a Friel
  30-minute threshold test after the health hold ends.
